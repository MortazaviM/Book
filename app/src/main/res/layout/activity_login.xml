<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">


    <Button
        android:textColor="@android:color/darker_gray"
        android:id="@+id/btn_signup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/login_margin_btn"
        android:background="@android:color/transparent"
        android:text="Dosen't have an account? Sign Up!" />

    <TextView
        android:id="@+id/txt_forget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/btn_signup"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/marginTop"
        android:text="Forget your password?" />


    <Button
        android:id="@+id/btn_signin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/txt_forget"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/marginTop"
        android:background="@drawable/roundedbutton"
        android:paddingLeft="@dimen/login_padding_btn"
        android:paddingRight="@dimen/login_padding_btn"
        android:shadowRadius="20"
        android:text="Sign in" />


    <EditText
        android:id="@+id/txt_pass"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/btn_signin"
        android:layout_marginTop="@dimen/marginTop"
        android:drawableLeft="@drawable/ic_locked"
        android:hint="Password" />


    <EditText
        android:id="@+id/txt_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/txt_pass"
        android:drawableLeft="@drawable/ic_envelope"
        android:hint="E-mail" />


    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/txt_email"
        android:src="@drawable/ic_bookshelf_logo" />


</RelativeLayout>
